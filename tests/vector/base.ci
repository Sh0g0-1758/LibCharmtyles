mainmodule base
{
    extern module libcharmtyles;
    PUPable ct::unary_op_wrapper;
    PUPable ct::negate_op_wrapper;

    mainchare Main
    {
        entry Main(CkArgMsg*);
    
        entry [threaded] void benchmark();
        entry [threaded] void check_kokkos();
    };
}
